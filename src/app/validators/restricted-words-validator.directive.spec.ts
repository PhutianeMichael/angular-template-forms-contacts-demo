import { RestrictedWordsValidatorDirective } from './restricted-words-validator.directive';

describe('RestrictedWordsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictedWordsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
